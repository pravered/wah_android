<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:layout_marginRight="8dp"
    android:background="@drawable/chips_contacts_bg"
    android:orientation="horizontal">

    <wahCustomViews.view.WahImageView xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/contact_thumbnail"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="center_vertical"
        android:src="@drawable/user_placeholder"
        app:placeholder_img="@drawable/user_placeholder"
        android:scaleType="centerCrop"
        app:is_circular="true"
        app:img_border_color="@color/app_orange"
        app:img_border_width="1dp" />

    <TextView
        android:id="@+id/contact_name"
        fontPath="fonts/roboto/Roboto-Bold.ttf"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="8dp"
        android:text="Kapil Mangtani"
        android:textColor="@color/app_orange"
        android:textSize="12sp" />

</LinearLayout>